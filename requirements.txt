streamlit>=1.40.0
pandas>=2.2.3
numpy>=2.1.0
scipy>=1.14.0
requests>=2.31.0
requests-cache>=1.2.0
plotly>=5.24.0
orjson>=3.9.0
python-dateutil>=2.9.0
python-dotenv>=1.0.0
yfinance>=0.2.40
feedparser>=6.0.10
fpdf2>=2.7.9
openpyxl>=3.1.2
# 2026-04-26 audit-round-1: real broker wiring. Optional at runtime —
# integrations/broker_alpaca_paper.py falls back to mock if missing.
alpaca-py>=0.30.0
# 2026-05-01 Sprint 2.7: Playwright-based AUM extractors for issuers
# whose product pages are JS-rendered (Franklin Templeton) or whose
# static-HTML route fails. The runtime probe in
# `integrations.issuer_extractors_playwright.is_playwright_available()`
# returns False if the python module OR the chromium binary is absent —
# in which case the chain silently falls through. NO blocking import
# at app cold-start. CLAUDE.md §10 (no-fallback), §11 (env-scoped).
#
# IMPORTANT: Streamlit Cloud needs `python -m playwright install
# chromium` to run AFTER pip install resolves this dep. Either:
#   (a) include `setup.sh` with `python -m playwright install chromium`
#       in repo root (Streamlit Cloud auto-runs setup.sh post-pip), or
#   (b) document the manual step in Settings → Advanced → Runtime cmd.
# packages.txt sibling file lists the apt deps chromium needs.
playwright>=1.42
