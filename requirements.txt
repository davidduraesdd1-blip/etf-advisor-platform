streamlit>=1.40.0
pandas>=2.2.3
numpy>=2.1.0
scipy>=1.14.0
requests>=2.31.0
requests-cache>=1.2.0
plotly>=5.24.0
orjson>=3.9.0
python-dateutil>=2.9.0
python-dotenv>=1.0.0
yfinance>=0.2.40
feedparser>=6.0.10
fpdf2>=2.7.9
openpyxl>=3.1.2
# 2026-04-26 audit-round-1: real broker wiring. Optional at runtime —
# integrations/broker_alpaca_paper.py falls back to mock if missing.
alpaca-py>=0.30.0
# 2026-05-01 Sprint 2.7: Playwright-based AUM extractors for JS-rendered
# issuer pages (Franklin Templeton).
#
# DEPLOY-INFRA NOTE (2026-04-30 audit-fix): `playwright` is INTENTIONALLY
# commented out for Streamlit Cloud — their pip-install step fails on
# this wheel + chromium combination on the Community Cloud free tier.
#
# The chain is fully resilient: integrations.issuer_extractors_playwright
# runtime-probes `is_playwright_available()` and silently no-ops if the
# module is missing. Without Playwright we lose AUM lift on the 3
# Franklin Templeton tickers (which render em-dash per no-fallback
# policy) — but the app does NOT crash on cold-start.
#
# Re-enable for local dev / self-hosted runners by uncommenting and
# running `python -m playwright install chromium` in the venv. Tracked
# for Streamlit-Cloud re-enable in pending_work.md when their Playwright
# support story stabilizes.
# playwright>=1.42
